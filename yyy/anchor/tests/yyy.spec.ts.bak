// import * as anchor from '@coral-xyz/anchor'
// import { Program } from '@coral-xyz/anchor'
// import { Keypair } from '@solana/web3.js'
// import { Yyy } from '../target/types/yyy'

// describe('yyy', () => {
//   // Configure the client to use the local cluster.
//   const provider = anchor.AnchorProvider.env()
//   anchor.setProvider(provider)
//   const payer = provider.wallet as anchor.Wallet

//   const program = anchor.workspace.Yyy as Program<Yyy>

//   const yyyKeypair = Keypair.generate()

//   it('Initialize Yyy', async () => {
//     await program.methods
//       .initialize()
//       .accounts({
//         yyy: yyyKeypair.publicKey,
//         payer: payer.publicKey,
//       })
//       .signers([yyyKeypair])
//       .rpc()

//     const currentCount = await program.account.yyy.fetch(yyyKeypair.publicKey)

//     expect(currentCount.count).toEqual(0)
//   })

//   it('Increment Yyy', async () => {
//     await program.methods.increment().accounts({ yyy: yyyKeypair.publicKey }).rpc()

//     const currentCount = await program.account.yyy.fetch(yyyKeypair.publicKey)

//     expect(currentCount.count).toEqual(1)
//   })

//   it('Increment Yyy Again', async () => {
//     await program.methods.increment().accounts({ yyy: yyyKeypair.publicKey }).rpc()

//     const currentCount = await program.account.yyy.fetch(yyyKeypair.publicKey)

//     expect(currentCount.count).toEqual(2)
//   })

//   it('Decrement Yyy', async () => {
//     await program.methods.decrement().accounts({ yyy: yyyKeypair.publicKey }).rpc()

//     const currentCount = await program.account.yyy.fetch(yyyKeypair.publicKey)

//     expect(currentCount.count).toEqual(1)
//   })

//   it('Set yyy value', async () => {
//     await program.methods.set(42).accounts({ yyy: yyyKeypair.publicKey }).rpc()

//     const currentCount = await program.account.yyy.fetch(yyyKeypair.publicKey)

//     expect(currentCount.count).toEqual(42)
//   })

//   it('Set close the yyy account', async () => {
//     await program.methods
//       .close()
//       .accounts({
//         payer: payer.publicKey,
//         yyy: yyyKeypair.publicKey,
//       })
//       .rpc()

//     // The account should no longer exist, returning null.
//     const userAccount = await program.account.yyy.fetchNullable(yyyKeypair.publicKey)
//     expect(userAccount).toBeNull()
//   })
// })
